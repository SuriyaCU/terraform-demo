# Terraform Demo Project

This repository contains a hands-on Terraform project for AWS.  
It demonstrates creating an EC2 instance with an optional security group using Infrastructure as Code (IaC).  

## Usage
1. `terraform init`  
2. `terraform plan`  
3. `terraform apply`  
4. `terraform destroy`
